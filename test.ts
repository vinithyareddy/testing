// GET /api/platforms → returns mock data for platform list (used in platform dropdowns / dashboards)
// GET /api/summary → returns top summary counts like advisory lots, open/in-progress/closed totals
// GET /api/progress → returns a percentage progress value for the central gauge component
// GET /api/kpis → returns KPI metrics such as Engagements, Users, Views, Leads for dashboard tiles
// GET /api/impact-by-platform → returns impact score data for each platform (bar/heat map)
// GET /api/sections → returns summary of key sections like “Business & Supply Chain Operations”
// GET /api/cards → returns bottom-right cards data (Opportunities, Spend, Pipeline)
// GET /health → health-check endpoint confirming the mock API server is up