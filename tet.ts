if (
  resData.title?.trim().toLowerCase() === element2.category?.trim().toLowerCase() &&
  element[child.measureQuery.code]?.toLowerCase?.() === element2.name?.toLowerCase?.()
) {
  element.checked = true;
}
